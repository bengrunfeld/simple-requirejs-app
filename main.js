$(function(){
  require(["one", "two", "three"], function(one, two, three){
      if (one, two, three) {
        console.log(one, two, three);
        console.log('Hooray! It works!!');
      };
  });
});
